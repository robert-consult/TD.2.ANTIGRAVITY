# Headlamp Ingress — Superadmin-Only Access via Nginx
# Exposes Headlamp under /headlamp path, protected by app-session auth + optional CIDR allowlist.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tradehub-headlamp-ingress
  namespace: tradehub
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/auth-url: "http://tradehub-api.tradehub.svc.cluster.local/api/admin/ops/ingress-auth?resource=headlamp"
    nginx.ingress.kubernetes.io/auth-response-headers: "X-TradeHub-Admin-Id,X-TradeHub-Admin-Email"
    nginx.ingress.kubernetes.io/limit-rps: "30"
    nginx.ingress.kubernetes.io/limit-connections: "10"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tradehub.example.com
      secretName: tradehub-tls
  rules:
    - host: tradehub.example.com
      http:
        paths:
          - path: /headlamp(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: tradehub-headlamp
                port:
                  number: 4466
