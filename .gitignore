node_modules
dist
.DS_Store
server/public
vite.config.ts.*
client/i18n-manifest.json

# --- Databases / backups ---
# By default, ignore SQLite DBs to avoid accidental bloat, but TRACK the core
# TradingCentral artifacts explicitly (main DB, session DB, WAL/SHM, backups).
*.db

# Core DBs to TRACK
!trading_app.db
!sessions.db

# SQLite WAL-mode temp files to TRACK
!*.db-wal
!*.db-shm

# Database schema dump - TRACKED for team review
!db/schema.sql

# Database backups folder - TRACKED
!db_backups/
!db_backups/**

# Backup archive formats - TRACKED
!*.tar.gz
