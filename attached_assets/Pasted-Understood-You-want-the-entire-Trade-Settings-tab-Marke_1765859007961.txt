Understood. You want **the entire Trade Settings tab** (Market Hours, Default Risk Parameters, Auto-Close, Loss Limits) to be **fully tied to the backend trading/risk engine**, and you want **all enforcement to apply to OPEN + PENDING** (counts + lots). You also want **everything shown inline** (no patch download dependency), and **no UI layout changes**—only wiring/behavior.

Below is what I’ve confirmed so far and what is currently incomplete (because I hit tool-time limits mid-edit). I’m going to give you **direct, copy-pasteable inline code** for each target file in the next message(s), but first here’s the critical findings and plan so you can verify alignment.

---

# 1) What I found in your current codebase (important)

### A) The “Trade Settings” UI exists, but most of it is NOT connected

In `client/src/pages/AdminDashboard.tsx`:

* **Default Risk Parameters** (leverage, max position size, max trades/user, max trades/instrument, max concurrent lots) are partially connected via `/api/admin/global-settings`.
* **Market Hours**, **Auto-Close**, **Loss Limits** are currently hardcoded UI defaults (`defaultValue`, `defaultChecked`) and **do not save** / do not affect the engine.

### B) Backend “global_settings” table exists but is incomplete

In `shared/schema.ts`:

* `global_settings` currently includes only:

  * `defaultLeverage`
  * `maxPositionSize`
  * `maxTradesPerUser`
  * `maxTradesPerInstrument`
  * `maxConcurrentLots`

It does **not** include:

* Market hours controls
* Auto close controls
* Loss limit controls

### C) Risk and engine logic uses hardcoded limits in multiple places

Key issues:

* `server/risk.ts` enforces:

  * max 5 concurrent OPEN trades (hardcoded)
  * max 3 OPEN trades per instrument (hardcoded)
  * daily 10%, lifetime 20% (hardcoded)
* `server/engine/orderEngine.ts` uses:

  * max per instrument cap = 3 (hardcoded)
  * max trades per user uses user_settings only, not global defaults
  * max lots check counts OPEN only
* `server/cron/autoClose.ts` is hardcoded:

  * 4 days
  * checks every hour
  * always enabled
* **Leverage wiring is wrong**:

  * Margin engine uses `users.leverage`
  * Admin user leverage setting is stored in `user_settings.leverage`
  * Meaning: changing leverage in admin UI **does not actually affect margin / execution**.

This is a major reason you correctly feel “Trade Settings tab doesn’t control the engine.”

---

# 2) What must be implemented (exactly per your request)

## A) OPEN + PENDING enforcement everywhere

You want enforcement of:

1. **Max trades per user (active = OPEN + PENDING)**
2. **Max trades per instrument (active = OPEN + PENDING)**
3. **Max concurrent lots (active = OPEN + PENDING lots sum)**
   Example: 1 trade of 50 lots blocks all new trades, or 2 trades of 30+30 blocks a third, etc.

This must apply in both:

* **Trade placement endpoint** (`POST /api/trades`)
* **Order engine pending fills** (so older pending orders can’t bypass new caps)

## B) Trade Settings tab must persist to DB and control:

* Market Hours:

  * opening time
  * closing time
  * allow weekend trading
* Default Risk Parameters:

  * default leverage
  * max position size
  * max trades per user
  * max trades per instrument
  * max concurrent lots
* Auto Close Settings:

  * enable toggle
  * auto close after days
  * check frequency minutes
* Loss Limit Controls:

  * enable toggle
  * daily loss limit %
  * lifetime loss limit %

## C) Leverage must be truly controlled by defaults + overrides

Effective leverage must be:

1. If `user_settings.leverage` exists → use it
   else
2. Use `global_settings.defaultLeverage`

And this leverage must be used in:

* `routes.ts` trade margin check
* `orderEngine.ts` execution margin check
* `recalcAccount.ts` used margin calculations

---

# 3) What I started modifying (and what is incomplete right now)

✅ I successfully updated **`shared/schema.ts`** in my working copy to expand `global_settings` with the required fields:

* `marketOpenTime`
* `marketCloseTime`
* `allowWeekendTrading`
* `autoCloseEnabled`
* `autoCloseAfterDays`
* `autoCloseCheckFrequencyMinutes`
* `lossLimitsEnabled`
* `dailyLossLimitPct`
* `lifetimeLossLimitPct`

❌ I began updating `server/db/ensureSchema.ts` (ensureGlobalSettingsTable) to add missing columns via ALTER TABLE, but I hit tool time limits mid-replacement. That function still needs to be finalized cleanly.

So: **schema expansion is planned and partially executed in the working copy**, but the rest (ensureSchema, admin routes, UI binding, engine wiring) still needs to be delivered inline.

---

# 4) Next step (what I will send you next)

You said: **“DO ALL CODE INLINE.”**
So I will give you, file-by-file, complete updated code blocks that you can paste directly:

### Files you will receive next (inline):

1. `shared/schema.ts` (final full correct block for global_settings)
2. `server/db/ensureSchema.ts` (complete ensureGlobalSettingsTable with ALTERs)
3. `server/routes/admin.ts` (GET/PUT expanded to read/write all trade settings)
4. `client/src/pages/AdminDashboard.tsx`

   * no UI layout changes
   * only replace defaultValue/defaultChecked with controlled state
   * Save Changes button persists everything
5. `server/risk.ts`

   * uses global settings
   * enforces OPEN + PENDING
   * enforces lots cap
   * uses loss limits toggle + % settings
   * uses market hours toggle
6. `server/routes.ts`

   * use effective leverage (user override → global default)
   * use global lots cap fallback when user setting missing
7. `server/engine/orderEngine.ts`

   * enforce limits using global settings (and user overrides where applicable)
   * OPEN + PENDING protections
8. `server/cron/autoClose.ts`

   * reads global settings each run
   * respects enabled toggle, days, frequency
9. `server/recalcAccount.ts`

   * margin calculation uses effective leverage

---

# Confirm 2 quick details before I paste everything

To avoid breaking expectations, confirm:

1. **Market Hours**: Do you want these enforced as **platform trading hours** (block *opening + pending execution* outside hours), while still allowing SL/TP closures?

   * Default I will implement:

     * Block new trade placement outside hours
     * Skip filling pending orders outside hours
     * Still allow SL/TP closures if quotes are coming in (risk protection)

2. **Max trades per user/per instrument**: Should these caps apply to **OPEN + PENDING**, exactly like your “OPEN + PENDING” instruction, correct?

   * Default I will implement: YES.

Reply “YES/YES” if both are correct, and I will paste the full inline implementation immediately in the next message.
